import { css } from 'styled-components';

const SIZES = {
  sm: css`
    --image-width: 152px;
    --image-height: 80px;
  `,
  md: css`
    --image-width: 100%;
    --image-height: auto;
  `
};

export default SIZES;
